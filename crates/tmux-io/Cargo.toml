[package]
name = "tmux-ai-io"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
tokio = { version = "1", features = ["full"] }
tmux-ai-parser = { path = "../parser" }
rand = "0.10.0"
regex = "1"
